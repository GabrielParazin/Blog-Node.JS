{{#if success_msg}}
    <div class="alert alert-success">{{success_msg}}</div> <!-- essas mensagens vem do middleware -->
{{/if}}

{{#if error_msg}}
    <div class="alert alert-danger">{{error_msg}}</div>
{{/if}}